' ================================================================================
' HOUR / MINUTE / SECOND サンプルプログラム
'  今日の時刻から時(HOUR関数) / 分(MINUTE関数) / 秒(SECOND関数) の値を、
'  それぞれ抽出します。
'				Copyright 2019 Interface Corporation
' ================================================================================

CLS

S$ = TIME$		' 今の時刻を求める

PRINT "今日の時刻:"; S$
PRINT "時の値:"; HOUR(S$)
PRINT "分の値:"; MINUTE(S$)
PRINT "秒の値:"; SECOND(S$)

END
