' ================================================================================
' MEMINFO サンプルプログラム 
'  MEMINFO関数で、システムのメモリ情報を取得し、表示します。
'				Copyright 2020 Interface Corporation
' ================================================================================

' 画面クリア
CLS

'システムの空きメモリ容量とシステムの搭載メモリを取得
PRINT "空き容量/搭載量 : "+STR$(ROUND(MEMINFO(0)/1024,2))+"/"+STR$(ROUND(MEMINFO(1)/1024))+"(MB)"
'AJANで使用しているメモリ量を取得
PRINT "   AJANの使用量 : "+STR$(MEMINFO(2)/1024)+"(MB)"

END

